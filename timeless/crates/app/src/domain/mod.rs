//! # 纯逻辑域（纯 Rust，零引擎依赖）
//!
//! 从旧 `timeless-domain` crate 增量迁移进来的模块统一放这里。
//!
//! 约束：
//! - 本子树内的模块**绝不** `use bevy::*`（也不引用 `crate::*` 下任何引擎模块），
//!   只能依赖 `std` 与纯数学类型（如 `glam`，经 bevy 再导出），保证可独立单测；
//! - 只承载「输入 → 输出」的纯函数与规则，不读写 ECS、不触发副作用；
//! - 日后若想恢复 crate 级隔离（零引擎依赖靠编译期强制），把本目录整体抽成
//!   新 workspace crate 即可（此时 `glam` 需改为直接依赖）。

pub mod combat;
